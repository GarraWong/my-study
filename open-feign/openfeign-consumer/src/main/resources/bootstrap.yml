server:
  port: 8100
spring:
  application:
    name: consumer
  cloud:
    nacos:
      config:
        # 配置中心地址
        server-addr: localhost:8848
      discovery:
        server-addr: localhost:8848